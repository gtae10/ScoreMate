spring:
  datasource:
    url: jdbc:mysql://localhost:3306/scoremate?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul
    username: scoremate
    password: 여기에_실제_비밀번호_입력
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

logging:
  level:
    ScoreMate.ScoreMate: debug
